export const firstnameOptions = {
  required: { value: true, message: "Введите имя" },
};

export const lastnameOptions = {
  required: { value: true, message: "Введите фамилию" },
};

export const middleNameOptions = {
  required: { value: true, message: "Введите отчество" },
};

export const regionOptions = {
  required: { value: true, message: "Введите область" },
};

export const cityOptions = {
  required: { value: true, message: "Введите город" },
};

export const addressOptions = {
  required: { value: true, message: "Введите адрес" },
};
